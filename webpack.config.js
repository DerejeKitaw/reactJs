var webpack =require("webpack");
var path= require("path");

// Setup distribuition directory
var DIST_DIR = path.resolve(__dirname, "dist");
// Setup source directory
var SRS_DIR = path.resolve(__dirname, "src");


